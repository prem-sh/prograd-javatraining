package sortingAlgorithms;

public class InsertionSort implements SortingAlgorithm {
   public static void ascending(int[] arr){
       for (int i = 0; i< arr.length; i++){
           int curr = arr[i];
           int j = i;
           while (j<arr.length && arr[j+1] < curr){

           }
       }
    }
}
